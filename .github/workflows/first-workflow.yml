name: My First Workflow

on:
  push:
    branches: [ "main" ]

jobs:
  run-one-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Workflow is running successfully 🚀"